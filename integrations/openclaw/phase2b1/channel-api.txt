=== sdk-channel-plugins sender sections ===
41:distinguishable until that encoding boundary; never infer picker intent from a
46:Expose a `message` adapter with `defineChannelMessageAdapter` from
57:`createChannelMessageAdapterFromOutbound(...)` instead of hand-writing another
64:`createAcceptedChannelDeliveryResult(...)` from
65:`openclaw/plugin-sdk/channel-inbound`. It combines native `results` followed by
74:Channel actions and adapter capabilities come from the selected plugin
76:filled from another plugin with the same channel ID. Prepared delivery handlers
154:Inbound receivers that defer platform acknowledgements should declare
168:The legacy `dispatchInboundReplyWithBase` helper remains available from the
169:deprecated `openclaw/plugin-sdk/inbound-reply-dispatch` compatibility shim.
174:### Inbound ingress (experimental)
176:Channels migrating inbound authorization can use the experimental
177:`openclaw/plugin-sdk/channel-ingress-runtime` subpath from runtime receive
179:facts, and access group config, then returns sender/route/command/activation
182:descriptor you pass to the resolver; do not serialize raw match values from
192:authoritative substitute. Never reconstruct evidence from sender, route, room,
202:from `openclaw/plugin-sdk/channel-outbound` unless they need a materially
216:鈥?a logical message key that differs from the transport delivery id (Telegram
223:outbound-echo registry from `openclaw/plugin-sdk/channel-outbound` instead of a
261:`createIngressEffectOnce(...)` from
291:`channels.example.accounts.*.allowFrom`. Deeper boundaries take precedence;
355:If your channel supports typing indicators outside inbound replies, expose
363:Resolve account media limits with `resolveChannelMediaMaxBytes(...)` from
385:fetch can use `createHostedOutboundMediaStore(...)` from
401:Inbound attachments use ordered facts, not parallel `Media*` fields. Normalize
402:channel records with `toInboundMediaFacts(...)` from
403:`openclaw/plugin-sdk/channel-inbound` and pass them as `media` when building the
404:inbound context. When a plugin must authorize local media reads, import
406:`getAgentScopedMediaLocalRootsForSources(...)` from the focused
412:Set `outbound.sendPayloadGroupsMedia: true` only when the payload sender owns
414:sender when its durable payload and reconciliation capabilities permit it.
417:Grouped senders must check the outbound context's `signal` before each physical
429:For send actions, preserve the trusted context's `onPlatformSendDispatch`,
431:`sendDurableMessageBatch(...)`. These fields come from the host, not action
449:order them from the narrowest parent to the broadest/base conversation.
466:stable dedupe key from `{ channel, to, accountId, threadId }`. The helper
477:identity, including a delivery `target` when it differs from the routing peer.
478:It must reuse the same precedence and provider identity grammar as inbound
487:  inbound claim handler.
493:Keep temporary unavailability distinct from `null`: an adapter restart is not
496:result from `openclaw/plugin-sdk/conversation-binding-inspection-runtime` for this
516:Resolve the answer from already-loaded account config or runtime state. This
536:Import `getSessionBindingService` from `openclaw/plugin-sdk/session-binding-runtime`.
544:details and explicit lifecycle settings separate from target metadata.
546:Use `resolveThreadBindingLifecycle(...)` from
556:`isPluginOwnedSessionBindingRecord(...)` from
557:`openclaw/plugin-sdk/conversation-binding-runtime` to distinguish them from
570:only - core no longer reads approval auth hooks from that object.
587:  initiating-surface/native-client state when it differs from same-chat
593:- If a channel can infer stable owner-like DM identities from existing config,
594:  use `createResolvedApproverActionAuthAdapter` from
598:  `markImplicitSameChatApprovalAuthorization({ authorized: true })` from
620:  infer this from `describeExecApprovalSetup`; pass the same helper explicitly
629:`createApproverRestrictedNativeApprovalCapability` from
662:- Use `settleApprovalReaction` from
673:  `buildSystemAgentApprovalResolvedText` from
686:- Use `createNativeApprovalControlRegistry` from
694:- Use `createNativeApprovalChannelRouteGates` from
721:  registry lets core bootstrap capability-driven handlers from channel
732:  their own "approval went to DMs / another channel" follow-up messages from
738:  not guess or rewrite exec vs plugin approval routing from channel-local
744:  infer kind from an ID.
782:  `promptResolvedAllowFrom`, `splitSetupEntries`, and the delegated
808:summaries. Do not start clients, listeners, or transport runtimes from the
816:Put those runtime pieces in modules loaded from `registerFull(...)`, runtime
821:Use `buildChannelAccountSchemaParts` from
826:those defaults to account entries or remove them from the root; the former
830:Use `refineChannelDmPolicy({ channelId, value, ctx })` from the same subpath
831:to validate the root policy against `allowFrom`. Pass `accountId` to validate
844:when that kind of account collection is empty. `preserveRootAllowFrom: true` removes an account wildcard
845:when the root contains restrictive sender entries, retaining explicit account
846:senders or falling back to the root list. These collection and allowlist rules
859:- `openclaw/plugin-sdk/inbound-envelope` and
860:  `openclaw/plugin-sdk/channel-inbound` for inbound route/envelope and
862:- `readAgentRunTerminalOutcome(dispatchResult)` from
863:  `openclaw/plugin-sdk/channel-inbound` when terminal reactions or status UI
864:  must distinguish a completed core agent run from a recovered failed run. It
870:- `createInboundEventDeliveryCorrelation(...)` from
871:  `openclaw/plugin-sdk/inbound-event-delivery` when successful outbound sends must
872:  retire an active inbound-event marker; create one tracker per channel and


=== feishu inbound example ===
/app/docs/plugins/architecture.md-211-- `sessionId`
/app/docs/plugins/architecture.md-212-- `agentId`
/app/docs/plugins/architecture.md:213:- trusted inbound `requesterSenderId`
/app/docs/plugins/architecture.md-214-
/app/docs/plugins/architecture.md-215-That matters for context-sensitive plugins. A channel can hide or expose message actions based on the active account, current room/thread/message, authoritative conversation type, or trusted requester identity without hardcoding channel-specific branches in the core `message` tool. Treat `chatType` as discovery scope supplied by the current inbound route, not something to infer again from an opaque channel id; it is absent when that route did not establish the conversation type.
/app/docs/plugins/architecture.md-216-
/app/docs/plugins/architecture.md-217-This is why embedded-runner routing changes are still plugin work: the runner is responsible for forwarding the current chat/session identity into the plugin discovery boundary so the shared `message` tool exposes the right channel-owned surface for the current turn.
/app/docs/plugins/architecture.md-218-


=== createChatChannelPlugin inbound ===
65:`openclaw/plugin-sdk/channel-inbound`. It combines native `results` followed by
66-logical `deliveryResults`, including a partial-delivery error's accepted subset.
67-A logical result's receipt takes precedence over its legacy message IDs.
68-The result carries a receipt, `messageIds` (including an empty array), and
69-`visibleReplySent: true`; routing fields stay in the receipt. Optional `content`
70-is passed through, and `kind` and `replyToId` use the receipt builder's rules.
71-Keep acceptance side effects, content joining,
72-suppression, and whether an identityless outcome needs a receipt in the adapter.
73-
74-Channel actions and adapter capabilities come from the selected plugin
75-registration. An omitted `actions`, `message`, or `outbound` surface is not
76-filled from another plugin with the same channel ID. Prepared delivery handlers
77-created inside a registry scope retain that handle when invoked after the caller
78-leaves the scope.
79-
80-Declare live and finalizer capabilities precisely - core uses these to decide
81-what a channel can do, and drift between the declared and actual behavior is a
82-contract test failure:
83-
84-| Surface                               | Values                                                                                           |
85-| ------------------------------------- | ------------------------------------------------------------------------------------------------ |
86-| `message.live.capabilities`           | `draftPreview`, `previewFinalization`, `progressUpdates`, `nativeStreaming`, `quietFinalization` |
87-| `message.live.finalizer.capabilities` | `finalEdit`, `normalFallback`, `discardPending`, `previewReceipt`, `retainOnAmbiguousFailure`    |
88-
89-Channels that finalize a draft preview in place should route the runtime logic
90-through `defineFinalizableLivePreviewAdapter(...)` plus
91-`deliverWithFinalizableLivePreviewAdapter(...)`, and keep the declared
92-capabilities backed by `verifyChannelMessageLiveCapabilityAdapterProofs(...)`
93-and `verifyChannelMessageLiveFinalizerProofs(...)` tests so native preview,
94-progress, edit, fallback/retention, cleanup, and receipt behavior cannot drift
95-silently.
96-
97-### Progress visibility acceptance
98-
99-Progress callbacks report what the operator can see, not merely what a plugin queued. Return
100-`true` after accepting visible progress and `false` while delivery is pending or when no visible
101-update occurred. Existing synchronous and asynchronous callbacks that return `void` remain
102-backward-compatible and are treated as visible; new acceptance-aware implementations should use
103-an explicit boolean.
104-
105-### Quiet progress presentation
--
169:deprecated `openclaw/plugin-sdk/inbound-reply-dispatch` compatibility shim.
170-Do not use it for new channel code; start with the `message` adapter, receipts,
171-and receive/send lifecycle helpers on `openclaw/plugin-sdk/channel-outbound`
172-instead.
173-
174-### Inbound ingress (experimental)
175-
176:Channels migrating inbound authorization can use the experimental
177-`openclaw/plugin-sdk/channel-ingress-runtime` subpath from runtime receive
178-paths. It accepts platform facts, raw allowlists, route descriptors, command
179-facts, and access group config, then returns sender/route/command/activation
180-projections plus the ordered ingress graph, while platform lookup and side
181-effects stay in the plugin. Keep plugin identity normalization in the
182-descriptor you pass to the resolver; do not serialize raw match values from
183-the resolved state or decision. See
184-[Channel ingress API](/plugins/sdk-channel-ingress) for the API design,
185-ownership boundary, and test expectations.
186-
187-Pass the exact resolver result to the host-injected registered context builder
188-as `channelIngress`. Results used for execution must include the final
189-agent/session/message/event `contextBinding`; decision-only resolver calls may
190-omit it. This preserves the native plugin's record-, epoch-, and scope-bound participant evidence through one-shot queued run admission without
191-exposing it in message context fields. The standalone public builder is not an
192-authoritative substitute. Never reconstruct evidence from sender, route, room,
193-account, thread, message, transport, or session values. Legacy adapters can explicitly pass
194-`channelIngress: "unsupported"` only when the path is source-proven to lack an
195-authoritative Phase 0 integration. Supported paths must pass the exact result;
196-omission is invalid production wiring. Missing, fake, stale, reused, or mixed
197-supported evidence projects as unknown, never as an allow signal.
198-
199-### Durable ingress and replay dedupe
200-
201-Channels adopting durable ingress should use `createChannelIngressMonitor`
202-from `openclaw/plugin-sdk/channel-outbound` unless they need a materially
203-different admission or pump contract. Enqueue the raw transport envelope at a
204-single receive chokepoint (no normalization at receive time), gate the
205-transport ack on the durable append for webhook transports, derive one
206-serialized lane per conversation, and mark the event complete at dispatch
207-adoption. The queue's primary key is `(queue_name, event_id)` and completion
208-tombstones the row instead of deleting it, so a late platform redelivery of
209-the same `event_id` is rejected durably for the tombstone retention window.
210-See [Channel outbound API](/plugins/sdk-channel-outbound#durable-ingress-monitors)
211-for the monitor API and shutdown contract.
212-
213-That tombstone is the layering rule for replay guards
214-(`openclaw/plugin-sdk/persistent-dedupe`): a drained channel keeps a separate
215-replay guard only when the guard's identity or retention exceeds the queue's
216-鈥?a logical message key that differs from the transport delivery id (Telegram
--
355:If your channel supports typing indicators outside inbound replies, expose
356-`heartbeat.sendTyping(...)` on the channel plugin. Core calls it with the
357-resolved heartbeat delivery target before the heartbeat model run starts and
358-uses the shared typing keepalive/cleanup lifecycle. Add
359-`heartbeat.clearTyping(...)` when the platform needs an explicit stop signal.
360-
361-### Media source params
362-
363-Resolve account media limits with `resolveChannelMediaMaxBytes(...)` from
364-`openclaw/plugin-sdk/account-helpers`. Pass the already-merged account's
365-`mediaMaxMb` through `resolveChannelLimitMb`; the helper applies the agent
366-default only when the account/channel limit is absent. Its optional byte result
367-must reach the actual media loader, capped by any transport ceiling. Preserve
368-the loader's existing default when no limit is configured.
369-
370-The focused account-helper import keeps setup and account resolution free of
371-media analysis runtimes. The old `media-runtime` export remains available for
372-existing external plugins, but new and bundled callers should use the focused import.
373-
374-If your channel adds message-tool params that carry media sources, expose
375-those param names through `plugin.actions.describeMessageTool(...).mediaSourceParams`.
376-Core uses that explicit list for sandbox path normalization and outbound
377-media-access policy, so plugins do not need shared-core special cases for
378-provider-specific avatar, attachment, or cover-image params.
379-
380-Prefer an action-keyed map such as `{ "set-profile": ["avatarUrl", "avatarPath"] }`
381-so unrelated actions do not inherit another action's media args. A flat array
382-still works for params intentionally shared across every exposed action.
383-


=== qa-channel (test channel?) ===
---
summary: "Synthetic Slack-class channel plugin for deterministic OpenClaw QA scenarios"
title: "QA channel"
read_when:
  - You are wiring the synthetic QA transport into a local or CI test run
  - You need the bundled qa-channel config surface
  - You are iterating on end-to-end QA automation
---

`qa-channel` is a repo-local synthetic message transport for automated OpenClaw QA (`extensions/qa-channel`, private package, excluded from packaged installs). It is not a production channel - it exists to exercise the same channel plugin boundary used by real transports while keeping state deterministic and fully inspectable.

## What it does

- Slack-class target grammar:
  - `dm:<user>`
  - `channel:<room>`
  - `group:<room>`
  - `thread:<room>/<thread>`
- Shared `channel:` and `group:` conversations are surfaced to agents as group/channel room turns, so they exercise the same visible-reply and message-tool routing policy used by Discord, Slack, Telegram, and similar transports.
- Message sends that omit `target` and `to` preserve the inbound conversation kind: direct, group, or channel.
- Sends in a threaded inbound conversation inherit its thread, including sends to the same explicit root target. `topLevel: true` or `threadId: null` suppresses implicit thread and reply inheritance. Explicit thread targets, thread IDs, and `replyTo` values are preserved.
- HTTP-backed synthetic bus for inbound message injection, outbound transcript capture, thread creation, reactions, edits, deletes, and search/read actions.
- Media-bearing streamed replies deliver attachments and the current tool trace together. A later identical text-only final is suppressed only when its caption and tool trace were already delivered successfully.
- Host-side self-check runner that writes a Markdown report to `.artifacts/qa-e2e/`.

## Config

```json
{
  "channels": {
    "qa-channel": {
      "baseUrl": "http://127.0.0.1:43123",
      "botUserId": "openclaw",
      "botDisplayName": "OpenClaw QA",
      "allowFrom": ["*"],
      "pollTimeoutMs": 1000
    }
  }
}
```

Account keys:

- `enabled` - master toggle for this account.
- `name` - optional display label.
- `responsePrefix` - automatic reply prefix; account overrides win. Accepts a literal, `"auto"` for the agent identity name, a template such as `"[{model}]"`, or `""` to disable an inherited prefix.
- `baseUrl` - synthetic bus URL. The account counts as configured once this is set.
- `botUserId` - synthetic bot user id used in target grammar (default: `openclaw`).
- `botDisplayName` - display name for outbound messages (default: `OpenClaw QA`).
- `pollTimeoutMs` - long-poll wait window. Integer between 100 and 30000 (default: 1000).
- `mediaMaxMb` - per-attachment limit in MiB for inbound bytes and outbound files. Named accounts override the channel root, then `agents.defaults.mediaMaxMb` supplies the fallback. If none is set, existing media-store and loader defaults apply. An oversized inbound attachment becomes an unavailable-attachment notice for the model. The channel loads each outbound batch before publishing it, so a failed attachment prevents that batch from being sent. Shared reply preparation can first remove oversized local files and retain valid attachments with a warning.
- `allowFrom` - sender allowlist (user ids or `"*"`; default: `["*"]`). DMs are
  always `open` policy; allowlisted group policy also uses these synthetic
  sender ids.
- `groupPolicy` - shared-room policy: `"open"` (default), `"allowlist"`, or
  `"disabled"`.
- `groupAllowFrom` - optional shared-room sender allowlist. When omitted under
  `"allowlist"`, QA Channel falls back to `allowFrom`.
- `groups.<room>.requireMention` - require a bot mention before replying in a
  specific group/channel room (default: false). `groups."*"` sets the default;
  per-room `tools` / `toolsBySender` set tool policy overrides.
- `defaultTo` - fallback target when none is supplied.
- `actions.messages` / `actions.reactions` / `actions.search` / `actions.threads` - per-action tool gating.

Multi-account keys at the top level:

- `accounts` - record of named per-account overrides keyed by account id.
- `defaultAccount` - preferred account id when multiple are configured.

## Runners

Host-side self-check (writes a Markdown report under `.artifacts/qa-e2e/`):

```bash
pnpm qa:e2e
```

This routes through `qa-lab`, starts the in-repo QA bus, boots the `qa-channel` runtime slice, and runs a deterministic self-check.

Full repo-backed scenario suite:

```bash
pnpm openclaw qa suite
```

The isolated `channel-participant-identity-inspection` scenario enables
execution identity before startup, exercises DM, group, senderless, same- and
mixed-participant collect paths, proves an ingress rejection creates no audit
rows, and compares JSON plus human CLI inspection across Gateway restart:

```bash
pnpm openclaw qa suite --scenario channel-participant-identity-inspection
```

Runs scenarios in parallel against the QA gateway lane. See [QA overview](/concepts/qa-e2e-automation) for scenarios, profiles, and provider modes.

Docker-backed QA site (gateway + QA Lab debugger UI in one stack):

```bash
pnpm qa:lab:up
```

Builds the QA site, starts the Docker-backed gateway + QA Lab stack, and prints the QA Lab URL. From there you can pick scenarios, choose the model lane, launch individual runs, and watch results live. The QA Lab debugger is separate from the shipped Control UI bundle.

## Related

- [QA overview](/concepts/qa-e2e-automation) - overall stack, transport adapters, the Matrix live lane, and scenario authoring
- [Pairing](/channels/pairing)
- [Groups](/channels/groups)
- [Channels overview](/channels)


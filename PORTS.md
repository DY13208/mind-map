# 端口说明

## Docker 启动

运行 `Start-Docker.bat` 或 `docker compose up -d --build` 时：

| 主机端口 | 容器端口 | 服务 | 监听范围 | 说明 |
| --- | --- | --- | --- | --- |
| `${MIND_MAP_PORT:-8080}` | `80` | Nginx 网关 | `0.0.0.0` | 浏览器页面以及 `/collab/`、`/api/`、`/ai/`、`/mcp` 的统一入口 |
| `${PGPORT:-15432}` | `5432` | PostgreSQL | `127.0.0.1` | 仅供本机调试数据库，不对局域网开放 |

应用容器内部还会监听以下端口，但它们**没有映射到主机**：

| 容器内部端口 | 服务 | 对外访问方式 |
| --- | --- | --- |
| `1234` | 协同 WebSocket 与文件 API | 由网关代理到 `/collab/` 和 `/api/` |
| `3456` | AI 服务 | 由网关代理到 `/ai/` |
| `3847` | MCP HTTP 服务 | 由网关代理到 `/mcp` |
| `5432` | PostgreSQL | 仅容器网络内部访问 |

因此 Docker 模式下，局域网同事只需要访问 `http://主机IP:8080`（如果 `.env` 修改了 `MIND_MAP_PORT`，使用修改后的端口）。

## 开发启动

运行 `start.bat` 或 `启动.bat`，选择“启动全部服务”时：

| 端口 | 协议/服务 | 默认地址 | 监听范围 |
| --- | --- | --- | --- |
| `8081` | Vue 开发页面 | `http://主机IP:8081` | `0.0.0.0` |
| `1234` | 协同 WebSocket 与文件 API | `ws://主机IP:1234`、`http://主机IP:1234/api/` | `0.0.0.0` |
| `3456` | AI 服务 | `http://主机IP:3456` | `0.0.0.0` |
| `3847` | MCP HTTP 服务 | `http://主机IP:3847/mcp` | `0.0.0.0` |
| `${PGPORT:-15432}` | PostgreSQL（Docker Compose 辅助启动） | `127.0.0.1:15432` | `127.0.0.1` |

开发启动会先停止占用 `8081`、`1234`、`3456`、`3847` 的旧项目进程，再启动对应服务。

